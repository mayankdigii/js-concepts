let myAge = 17;
let isOfAge = false;

if (myAge > 18) {
    isOfAge = true;
} else {
    isOfAge = false;
}


console.log('isOfAge', isOfAge)

// Using the Ternary Operator 

const isOfAge2 = myAge > 18 ? true : false;

console.log('isOfAge 2', isOfAge2);